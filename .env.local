# MongoDB Connection
# Replace with your MongoDB Atlas connection string or local MongoDB URI
# Note: If password contains special characters like @, encode them (e.g., @ becomes %40)
MONGODB_URI=mongodb+srv://samnang:Nang%402311@bugee.8pvhedu.mongodb.net/?retryWrites=true&w=majority

# Database Name (optional - defaults to 'wedding')
MONGODB_DB_NAME=wedding

# Base URL for invitation links (optional)
# For local development, leave as is
# For production, replace with your domain: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000
